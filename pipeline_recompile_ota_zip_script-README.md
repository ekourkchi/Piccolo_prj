* pull docker image "asmita08/segger-nrfsdk-ota:latest"
* Place the `webe-ota` folder in the same directory as the `pipeline_recompile_ota_zip_script.py` script on your local machine. Note : make sure to keep the name of webe-ota dir as `webe-ota`
* Also copy the webe firmware folder, and  rename it to `webe-dir`and place it as in same folder as `pipeline_recompile_ota_zip_script.py` script on your local machine
* Run the script as `./pipeline_recompile_ota_zip_script.py`
* Output will be in `output` dir 