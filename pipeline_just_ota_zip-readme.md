* pull docker image "asmita08/segger-nrfsdk-ota:latest"
* Place the webe-ota folder in the same directory as the `pipeline_just_ota_zip.py` script on your local machine.
* Run the script as `./pipeline_just_ota_zip.py <path-to-hex-file>`
* Output will be in `output` dir 